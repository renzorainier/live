rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Admin controls - allow read/write for the totem overlay display
    match /adminControls/{document=**} {
      allow read, write: if true;
    }
  }
}
